# Notes

**Status:** draft — seeded from eRS3CmvrOvA

## References

- `eRS3CmvrOvA/screenshots/frame_0665000ms.jpg` — Two visible circles (analytics → clock), arrow between, "Returns a compact index" label. Third circle partially off right edge (3-step flow animating in).

## Testing

- Test 1: not run
- Fresh-session test: not run

## Open Questions

- The circles in the reference appear to use teal-green glow rather than pure teal (#10B981 family) — confirm this is the right accent versus the more blue-teal (#0EA5E9) used elsewhere.
- The second circle (clock icon) appears slightly blurred/faded — possibly it represents the "current" or "next" step that hasn't been revealed yet. Consider a variant where nodes reveal progressively.
- The deep green gradient background in frame_0665 is more saturated/green than other clips — this clip may use a slightly different background atmosphere. Note as a variant: `{bg_tone}` = `"deep green"` vs `"neutral near-black"`.
